- name: install multiple packages through loops 
  hosts: localhost
  tasks:
  - name: install packages
    ansible.builtin.yum:
     name: "{{item}}"
    loop:
    - nginx
    - postfix
    - mailx
    - cyrus-sasl-plain