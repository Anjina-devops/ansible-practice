- name: install nginx and run 
  hosts: web
  become: yes
  tasks:
  - name: install nginx
    ansible.builtin.yum:
     name: nginx
     state: installed
  - name: start nginx
    ansible.builtin.services:
     name: nginx
     state: started
         


